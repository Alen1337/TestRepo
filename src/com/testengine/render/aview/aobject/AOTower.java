package com.testengine.render.aview.aobject;

import com.testengine.game.object.tower.Tower;

import java.awt.*;

public class AOTower {

    public static void render(Graphics g, Tower tower) {
        /*g.clearRect(0,0,renderPanel.getWidth(),renderPanel.getHeight());
        g.setColor(Color.BLUE);
        g.fillRect(renderPanel.getWidth() / 2 - 50, renderPanel.getHeight() / 5 - 50, 100, 40);
        g.setColor(Color.YELLOW);
        g.drawString("Back to Menu1",renderPanel.getWidth() / 2 - 35,renderPanel.getHeight() / 5 - 25);*/
    }
}
